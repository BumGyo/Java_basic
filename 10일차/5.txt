* 총 4가지로 외부 클래스와 내부 클래스 관계를 설정 가능

1. 인스턴스 변수 처럼 클래스를 정의하기
1) 구조 : 
   class 외부클래스이름 {
       // 멤버 변수와 메서드
       class 내부클래스이름 {
          // 외부 클래스에서 선언한 멤버 변수와 메서드들을 바로
          // 사용 가능
       }
   }
// main( ) 함수가 있는 클래스에서 내부클래스이름 클래스 이름을
// 사용하기 위해서는 먼저 외부 클래스 이름을 사용 + 점(.) +
// 내부 클래스 이름을 사용(간접) : 직접은 불가능



