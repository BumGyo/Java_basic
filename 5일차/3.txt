* 사용자가 국어 점수, 영어 점수, 수학 점수를 입력한 경우

1. 사용자가 숫자를 입력할거라고 예측(예상)
   -> 사용자는 문자를 입력할 수도 있습니다!!

1. 새로운 함수를 만들어서 함수 안에서 사용자가 입력한 점수가
   정수인지를 검사하기

1) static 함수를 만들기 : 
   반환형 : boolean : 사용자가 입력한 점수가 모두 숫자이면 참(true)
   을 반환하고 하나라도 문자가 있으면 거짓(false)을 반환

   public static boolean isValidScore(String score) {
	/*
           String 클래스가 갖고 있는 charAt( ) 함수를 호출하면
           한 글자씩 읽어올 수 있습니다!!
           예) if( 사용자가입력한문자열.equals("비교할문자열"))
           예) 사용자가 국어 점수를 70으로 입력한 경우 -> "70"
               String score = "70";
               char ch = score.charAt(0);
	 인덱스 0은 제일 왼쪽에 있는 숫자 7의 방 번호(위치 번호)
         인덱스 1은 두 번째 숫자 0의 방 번호(위치 번호)
        */
   }


2. 정규식(Regular Expression) : 규칙을 갖고 있는 식
   -> String 클래스가 갖고 있는 matches( ) 함수를 호출(사용)

